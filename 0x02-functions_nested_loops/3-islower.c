#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 * @c - checking
 *
 * Return: 1 if it is lowercase else its 0
 */

int _islower(int c);
{
if (c >= 'a' && c <= 'z')
	return (1);
else
	return (0);
}
